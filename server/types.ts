// server/types.tsimport type { Request } from "express";import type { UserRole } from "../shared/schema.js";// ✅ Standardized JWT claims used everywhereexport interface CustomJwtClaims {  sub: string;  email?: string;  name?: string;  [key: string]: any;}// ✅ Shared AuthenticatedRequest type used across middleware + routesexport interface AuthenticatedRequest extends Request {  user?: {    claims: CustomJwtClaims;    role: UserRole; // always enriched to a valid UserRole    department?: string;  };  sessionID?: string;}
