// complianceStatus.ts - endpoint to check system readinessimport { Router, type Request, type Response } from "express";import { runComplianceChecks } from "./compliance.js";const router: Router = Router(); // ✅ Explicit typerouter.get("/status", async (_req: Request, res: Response) => {  try {    const results = await runComplianceChecks();    res.json({      timestamp: new Date().toISOString(),      checks: results    });  } catch (err) {    console.error("Compliance status error:", err);    res.status(500).json({ message: "Failed to run compliance checks" });  }});export default router;
